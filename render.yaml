services:
  - type: web
    plan: free
    name: randre-todolist
    runtime: docker
    dockerfilePath: Dockerfile
    healthCheckPath: /
    envVars:
      - key: JAVA_OPTS
        value: "-Xmx512m -Xms256m"
    numInstances: 1
